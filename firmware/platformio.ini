; =============================================================================
; TurtlPass Firmware — Multi-board RP2040/RP2350 build setup
;
; Docs: https://arduino-pico.readthedocs.io/en/latest/platformio.html
; Platform: https://github.com/maxgerhardt/platform-raspberrypi.git
; =============================================================================

; =======================================
; PlatformIO Configuration
; =======================================
[platformio]
build_cache_dir = ~/.platformio/cache ; lets PlatformIO reuse compiled libraries across projects
core_dir = ~/.platformio/core

; =============================================================================
; Base environment: shared configuration for all RP2040/RP2350 boards
; =============================================================================
[env:base]
platform = https://github.com/maxgerhardt/platform-raspberrypi.git
board = pico
framework = arduino
build_type = release
; Earle Philhower’s Arduino-Pico core
board_build.core = earlephilhower
; Flash filesystem (Earle Philhower's Arduino-Pico core)
board_build.filesystem_size = 0.5m
; USB VID/PID: https://pid.codes/1209/FA55/
board_build.arduino.earlephilhower.usb_manufacturer = TurtlPass
board_build.arduino.earlephilhower.usb_product = TurtlPass Firmware
board_build.arduino.earlephilhower.usb_vid = 0x1209
board_build.arduino.earlephilhower.usb_pid = 0xFA55
; Compiler and SDK include paths
build_flags =
    -DTP_VERSION=\"3.1.0\"
    -DPIO_BOARD_NAME=\"$BOARD\"
    -DUSE_TINYUSB                       ; Adafruit TinyUSB
    -DFASTLED_INTERNAL                  ; Remove annoying FastLED pragma messages
; Common libraries
lib_deps =
    https://github.com/FastLED/FastLED.git#3.10.3
; Serial monitor
monitor_speed = 115200
monitor_filters = send_on_enter
monitor_echo = yes


; =============================================================================
; [Native Tests] — Run on host machine (no hardware)
; =============================================================================
; Run: 
; $ pio test -e native
; =============================================================================
; $ pio test -e native --filter native/test_key_derivation
; $ pio test -e native --filter native/test_encryption
; $ pio test -e native --filter native/test_led_manager
; $ pio test -e native --filter native/test_led_manager_contract
; =============================================================================
[env:native]
platform = native
test_framework = unity
test_build_src = true
test_filter = native/*
test_ignore = native/common/*
build_flags =
    -std=c++17
    -I src
    -I lib
    -Itest/native/common
    -D ARDUINO=10819        ; Fake Arduino version number
    -D BOOTSEL=false
    -I.pio/libdeps/native/FastLED/src
    -DFASTLED_INTERNAL
    -DFASTLED_ALLOW_INTERRUPTS=0
    -DFASTLED_REGISTER=     ; neutralize macro
lib_deps =
    https://github.com/FastLED/FastLED.git
build_src_filter =
    +<src/*>                ; include everything in src/
    +<lib/*>
    -<src/keyboard/*>
    -<src/ui/*>


; =============================================================================
; [Embedded Tests] — Run on RP2040 hardware (inherits base config)
; =============================================================================
; Run:
; $ pio test -e pico-tests
; =============================================================================
; $ pio test -e pico-tests --filter embedded/test_seedmanager_basic
; $ pio test -e pico-tests --filter embedded/test_seedmanager_crypto
; $ pio test -e pico-tests --filter embedded/test_seedmanager_eeprom
; $ pio test -e pico-tests --filter embedded/test_seedmanager_slots
; $ pio test -e pico-tests --filter embedded/test_storage_basic
; $ pio test -e pico-tests --filter embedded/test_storage_full
; $ pio test -e pico-tests --filter embedded/test_storage_roundtrip
; =============================================================================
[env:pico-tests]
extends = env:base
board = pico
framework = arduino
test_build_src = true
test_framework = unity
test_filter = embedded/
monitor_speed = 115200
; Keep firmware defines from base and override where needed
build_flags =
    ${env:base.build_flags}
    -DTP_PIN_LED=25
    -DTP_RGB_LED=0
    -I.pio/libdeps/pico-tests/FastLED/src
; Only build the components relevant to tests
build_src_filter =
    +<src/*>
    +<src/crypto/*>
    +<src/storage/*>
    -<src/main.cpp>     ; exclude firmware entry point
; Test libraries
lib_deps =
    https://github.com/FastLED/FastLED.git#3.10.3

; =======================================
; Raspberry Pi Pico W
; =======================================
; Run:
; $ pio test -e pico_w-tests
; =======================================
[env:pico_w-tests]
extends = env:base
board = rpipicow
test_build_src = true
test_framework = unity
test_filter = embedded/*
build_flags = ${env:base.build_flags}
build_src_filter = ${env:pico-tests.build_src_filter}


; =============================================================================
; [Build] Official Raspberry Pi Pico boards
; =============================================================================
; Build: 
; $ pio run -e pico
; Upload: 
; $ pio run -e pico -t upload
; =============================================================================

; =======================================
; Raspberry Pi Pico (RP2040)
; =======================================
[env:pico]
extends = env:base
board = pico
build_flags = ${env:base.build_flags}

; =======================================
; Raspberry Pi Pico W (RP2040 + Wi-Fi)
; =======================================
[env:rpipicow]
extends = env:base
board = rpipicow
build_flags = ${env:base.build_flags}

; =======================================
; Raspberry Pi Pico 2 (RP2350)
; =======================================
[env:rpipico2]
extends = env:base
board = rpipico2
build_flags = ${env:base.build_flags}

; =======================================
; Raspberry Pi Pico 2W (RP2350 + Wi-Fi)
; =======================================
[env:rpipico2w]
extends = env:base
board = rpipico2w
build_flags = ${env:base.build_flags}


; =============================================================================
; [Build] Generic RP2040/RP2350 Boards
; =============================================================================

; =======================================
; Generic RP2040
; =======================================
[env:generic]
extends = env:base
board = generic
build_flags =
    ${env:base.build_flags}
    -DTP_PIN_LED=25         ; Example, Pico built-in LED = GPIO 25
    -DTP_RGB_LED=0          ; Example, Pico built-in LED = Single-color

; =======================================
; Generic RP2350
; =======================================
[env:generic_rp2350]
extends = env:base
board = generic_rp2350
build_flags =
    ${env:base.build_flags}
    -DTP_PIN_LED=16         ; Example, RP2350-Zero built-in LED = GPIO 16
    -DTP_RGB_LED=1          ; Example, RP2350-Zero built-in LED = RGB


; -----------------------------------------------------------------------------
; [Build] All other boards RP2040/RP2350-based
; -----------------------------------------------------------------------------

; ---------------------------------------
; 0xCB Helios
; ---------------------------------------
[env:0xcb_helios]
extends = env:base
board = 0xcb_helios
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0

; ---------------------------------------
; Adafruit Feather RP2040 Adalogger
; ---------------------------------------
[env:adafruit_feather_adalogger]
extends = env:base
board = adafruit_feather_adalogger
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 CAN
; ---------------------------------------
[env:adafruit_feather_can]
extends = env:base
board = adafruit_feather_can
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 DVI
; ---------------------------------------
[env:adafruit_feather_dvi]
extends = env:base
board = adafruit_feather_dvi
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 Prop-Maker
; ---------------------------------------
[env:adafruit_feather_prop_maker]
extends = env:base
board = adafruit_feather_prop_maker
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 RFM
; ---------------------------------------
[env:adafruit_feather_rfm]
extends = env:base
board = adafruit_feather_rfm
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2350 Adalogger
; ---------------------------------------
[env:adafruit_feather_rp2350_adalogger]
extends = env:base
board = adafruit_feather_rp2350_adalogger
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2350 HSTX
; ---------------------------------------
[env:adafruit_feather_rp2350_hstx]
extends = env:base
board = adafruit_feather_rp2350_hstx
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 SCORPIO
; ---------------------------------------
[env:adafruit_feather_scorpio]
extends = env:base
board = adafruit_feather_scorpio
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 ThinkINK
; ---------------------------------------
[env:adafruit_feather_thinkink]
extends = env:base
board = adafruit_feather_thinkink
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040 USB Host
; ---------------------------------------
[env:adafruit_feather_usb_host]
extends = env:base
board = adafruit_feather_usb_host
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Feather RP2040
; ---------------------------------------
[env:adafruit_feather]
extends = env:base
board = adafruit_feather
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Floppsy
; ---------------------------------------
[env:adafruit_floppsy]
extends = env:base
board = adafruit_floppsy
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Fruit Jam RP2350
; ---------------------------------------
[env:adafruit_fruitjam]
extends = env:base
board = adafruit_fruitjam
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit ItsyBitsy RP2040
; ---------------------------------------
[env:adafruit_itsybitsy]
extends = env:base
board = adafruit_itsybitsy
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit KB2040
; ---------------------------------------
[env:adafruit_kb2040]
extends = env:base
board = adafruit_kb2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=17

; ---------------------------------------
; Adafruit MacroPad RP2040
; ---------------------------------------
[env:adafruit_macropad2040]
extends = env:base
board = adafruit_macropad2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Metro RP2350
; ---------------------------------------
[env:adafruit_metro_rp2350]
extends = env:base
board = adafruit_metro_rp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Metro RP2040
; ---------------------------------------
[env:adafruit_metro]
extends = env:base
board = adafruit_metro
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit QT Py RP2040
; ---------------------------------------
[env:adafruit_qtpy]
extends = env:base
board = adafruit_qtpy
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=12

; ---------------------------------------
; Adafruit STEMMA Friend RP2040
; ---------------------------------------
[env:adafruit_stemmafriend]
extends = env:base
board = adafruit_stemmafriend
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Adafruit Trinkey RP2040 QT
; ---------------------------------------
[env:adafruit_trinkeyrp2040qt]
extends = env:base
board = adafruit_trinkeyrp2040qt
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=27

; ---------------------------------------
; METE HOCA Akana R1
; ---------------------------------------
[env:akana_r1]
extends = env:base
board = akana_r1
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Amken BunnyBoard
; ---------------------------------------
[env:amken_bunny]
extends = env:base
board = amken_bunny
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Amken Revelop eS
; ---------------------------------------
[env:amken_revelop_es]
extends = env:base
board = amken_revelop_es
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Amken Revelop Plus
; ---------------------------------------
[env:amken_revelop_plus]
extends = env:base
board = amken_revelop_plus
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Amken Revelop
; ---------------------------------------
[env:amken_revelop]
extends = env:base
board = amken_revelop
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Arduino Nano RP2040 Connect
; ---------------------------------------
[env:arduino_nano_connect]
extends = env:base
board = arduino_nano_connect
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; ArtronShop RP2 Nano
; ---------------------------------------
[env:artronshop_rp2_nano]
extends = env:base
board = artronshop_rp2_nano
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; BIGTREETECH SKR-Pico
; ---------------------------------------
[env:bigtreetech_SKR_Pico]
extends = env:base
board = bigtreetech_SKR_Pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=7

; ---------------------------------------
; Breadstick Raspberry
; ---------------------------------------
; [env:breadstick_raspberry]
; extends = env:base
; board = breadstick_raspberry
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=0
; >>> ERROR: undefined reference to `SPI'

; ---------------------------------------
; BridgeTek IDM2040-43A
; ---------------------------------------
[env:bridgetek_idm2040_43a]
extends = env:base
board = bridgetek_idm2040_43a
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=25

; ---------------------------------------
; BridgeTek IDM2040-7A
; ---------------------------------------
[env:bridgetek_idm2040_7a]
extends = env:base
board = bridgetek_idm2040_7a
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=25

; ---------------------------------------
; iLabs Challenger 2040 LoRa
; ---------------------------------------
[env:challenger_2040_lora]
extends = env:base
board = challenger_2040_lora
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=15 -DSS=PIN_SPI0_SS

; ---------------------------------------
; iLabs Challenger 2040 LTE
; ---------------------------------------
[env:challenger_2040_lte]
extends = env:base
board = challenger_2040_lte
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=15 -DSS=PIN_SPI0_SS

; ---------------------------------------
; iLabs Challenger 2040 NFC
; ---------------------------------------
; [env:challenger_2040_nfc]
; extends = env:base
; board = challenger_2040_nfc
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=14 -DSS=PIN_SPI0_SS
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.

; ---------------------------------------
; iLabs Challenger 2040 SD/RTC
; ---------------------------------------
[env:challenger_2040_sdrtc]
extends = env:base
board = challenger_2040_sdrtc
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=18 -DSS=PIN_SPI0_SS

; ---------------------------------------
; iLabs Challenger 2040 SubGHz
; ---------------------------------------
[env:challenger_2040_subghz]
extends = env:base
board = challenger_2040_subghz
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=15 -DSS=PIN_SPI0_SS

; ---------------------------------------
; iLabs Challenger 2040 UWB
; ---------------------------------------
[env:challenger_2040_uwb]
extends = env:base
board = challenger_2040_uwb
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=18 -DSS=PIN_SPI0_SS

; ---------------------------------------
; iLabs Challenger 2040 WiFi/BLE
; ---------------------------------------
; [env:challenger_2040_wifi_ble]
; extends = env:base
; board = challenger_2040_wifi_ble
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=13
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.

; ---------------------------------------
; iLabs Challenger 2040 WiFi
; ---------------------------------------
; [env:challenger_2040_wifi]
; extends = env:base
; board = challenger_2040_wifi
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=11 -DSS=PIN_SPI0_SS
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.

; ---------------------------------------
; iLabs Challenger 2040 WiFi6/BLE
; ---------------------------------------
; [env:challenger_2040_wifi6_ble]
; extends = env:base
; board = challenger_2040_wifi6_ble
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=13
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.

; ---------------------------------------
; iLabs Challenger 2350 BConnect
; ---------------------------------------
; [env:challenger_2350_bconnect]
; extends = env:base
; board = challenger_2350_bconnect
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=13
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.


; ---------------------------------------
; iLabs Challenger 2350 WiFi/BLE
; ---------------------------------------
[env:challenger_2350_wifi6_ble5]
extends = env:base
board = challenger_2350_wifi6_ble5
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=13

; ---------------------------------------
; iLabs Challenger NB 2040 WiFi
; ---------------------------------------
; [env:challenger_nb_2040_wifi]
; extends = env:base
; board = challenger_nb_2040_wifi
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=15 -DSS=PIN_SPI0_SS
; >>> ERROR: 
; This Challenger board’s variant file defines a macro called NEOPIXEL. 
; Meanwhile, the FastLED library also defines a C++ class template named NEOPIXEL.
; Isn't possible to fix it by simply un-defining the macro, because the conflict isn’t happening in our code at all.

; ---------------------------------------
; iLabs Connectivity 2040 LTE/WiFi/BLE
; ---------------------------------------
[env:connectivity_2040_lte_wifi_ble]
extends = env:base
board = connectivity_2040_lte_wifi_ble
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=19

; ---------------------------------------
; Cytron IRIV IO Controller
; ---------------------------------------
[env:cytron_iriv_io_controller]
extends = env:base
board = cytron_iriv_io_controller
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Cytron Maker Nano RP2040
; ---------------------------------------
[env:cytron_maker_nano_rp2040]
extends = env:base
board = cytron_maker_nano_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Cytron Maker Pi RP2040
; ---------------------------------------
[env:cytron_maker_pi_rp2040]
extends = env:base
board = cytron_maker_pi_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Cytron Maker Uno RP2040
; ---------------------------------------
[env:cytron_maker_uno_rp2040]
extends = env:base
board = cytron_maker_uno_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Cytron Motion 2350 Pro
; ---------------------------------------
[env:cytron_motion_2350_pro]
extends = env:base
board = cytron_motion_2350_pro
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; DatanoiseTV PicoADK v2
; ---------------------------------------
[env:datanoisetv_picoadk_v2]
extends = env:base
board = datanoisetv_picoadk_v2
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; DatanoiseTV PicoADK
; ---------------------------------------
[env:datanoisetv_picoadk]
extends = env:base
board = datanoisetv_picoadk
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; DB-RP2350
; ---------------------------------------
; [env:db_rp2350]
; extends = env:base
; board = db_rp2350
; build_flags = ${env:base.build_flags}
; >>> ERROR: This board doesn't support arduino framework!

; ---------------------------------------
; Degz Robotics Suibo RP2040
; ---------------------------------------
[env:degz_suibo]
extends = env:base
board = degz_suibo
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; DFRobot Beetle RP2040
; ---------------------------------------
[env:dfrobot_beetle_rp2040]
extends = env:base
board = dfrobot_beetle_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; L'atelier d'Arnoz DudesCab
; ---------------------------------------
[env:DudesCab]
extends = env:base
board = DudesCab
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; ElectronicCats HunterCat NFC RP2040
; ---------------------------------------
[env:electroniccats_huntercat_nfc]
extends = env:base
board = electroniccats_huntercat_nfc
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; EVN Alpha
; ---------------------------------------
[env:evn_alpha]
extends = env:base
board = evn_alpha
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; ExtremeElectronics RC2040
; ---------------------------------------
[env:extelec_rc2040]
extends = env:base
board = extelec_rc2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; DeRuiLab FlyBoard2040Core
; ---------------------------------------
[env:flyboard2040_core]
extends = env:base
board = flyboard2040_core
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; GeeekPi RP2040 Plus
; ---------------------------------------
[env:geeekpi_rp2040_plus]
extends = env:base
board = geeekpi_rp2040_plus
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; GroundStudio Marble Pico
; ---------------------------------------
[env:groundstudio_marble_pico]
extends = env:base
board = groundstudio_marble_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; iLabs RPICO32
; ---------------------------------------
[env:ilabs_rpico32]
extends = env:base
board = ilabs_rpico32
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DSS=17

; ---------------------------------------
; Architeuthis Flux Jumperless
; ---------------------------------------
[env:jumperless_v1]
extends = env:base
board = jumperless_v1
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Architeuthis Flux Jumperless V5
; ---------------------------------------
[env:jumperless_v5]
extends = env:base
board = jumperless_v5
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Melopero Cookie RP2040
; ---------------------------------------
[env:melopero_cookie_rp2040]
extends = env:base
board = melopero_cookie_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Melopero Shake RP2040
; ---------------------------------------
[env:melopero_shake_rp2040]
extends = env:base
board = melopero_shake_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Makerbase MKS THR36
; ---------------------------------------
[env:mksthr36]
extends = env:base
board = mksthr36
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=20

; ---------------------------------------
; Makerbase MKS THR42
; ---------------------------------------
[env:mksthr42]
extends = env:base
board = mksthr42
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=20

; ---------------------------------------
; MyMakers RP2350B
; ---------------------------------------
; [env:MyRP_2350B]
; extends = env:base
; board = MyRP_2350B
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1
; >>> ERROR: Unknown board ID 'MyRP_2350B'

; ---------------------------------------
; MyMakers RP2040
; ---------------------------------------
[env:MyRP_bot]
extends = env:base
board = MyRP_bot
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Arduino Nano RP2040 Connect
; ---------------------------------------
[env:nanorp2040connect]
extends = env:base
board = nanorp2040connect
build_flags = ${env:base.build_flags}
    -DTP_RGB_3PIN_LED=1
    -DTP_PIN_LED_R=20
    -DTP_PIN_LED_G=21
    -DTP_PIN_LED_B=13

; ---------------------------------------
; Neko Systems BL2040 Mini [NO-LED]
; ---------------------------------------
[env:nekosystems_bl2040_mini]
extends = env:base
board = nekosystems_bl2040_mini
build_flags = ${env:base.build_flags}

; ---------------------------------------
; Newsan Archi
; ---------------------------------------
[env:newsan_archi]
extends = env:base
board = newsan_archi
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=24

; ---------------------------------------
; nullbits Bit-C PRO
; ---------------------------------------
[env:nullbits_bit_c_pro]
extends = env:base
board = nullbits_bit_c_pro
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Olimex Pico2XL
; ---------------------------------------
[env:olimex_pico2xl]
extends = env:base
board = olimex_pico2xl
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Olimex Pico2XXL
; ---------------------------------------
[env:olimex_pico2xxl]
extends = env:base
board = olimex_pico2xxl
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Olimex RP2040-Pico30
; ---------------------------------------
[env:olimex_rp2040pico30]
extends = env:base
board = olimex_rp2040pico30
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Pimoroni Explorer
; ---------------------------------------
[env:pimoroni_explorer]
extends = env:base
board = pimoroni_explorer
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25 -DSS=17

; ---------------------------------------
; Pimoroni PGA2040
; ---------------------------------------
[env:pimoroni_pga2040]
extends = env:base
board = pimoroni_pga2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Pimoroni PGA2350
; ---------------------------------------
[env:pimoroni_pga2350]
extends = env:base
board = pimoroni_pga2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Pimoroni PicoPlus2
; ---------------------------------------
[env:pimoroni_pico_plus_2]
extends = env:base
board = pimoroni_pico_plus_2
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Pimoroni PicoPlus2W
; ---------------------------------------
[env:pimoroni_pico_plus_2w]
extends = env:base
board = pimoroni_pico_plus_2w
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Pimoroni Plasma2040
; ---------------------------------------
[env:pimoroni_plasma2040]
extends = env:base
board = pimoroni_plasma2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25 -DSS=17

; ---------------------------------------
; Pimoroni Plasma2350
; ---------------------------------------
[env:pimoroni_plasma2350]
extends = env:base
board = pimoroni_plasma2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25 -DSS=17

; ---------------------------------------
; Pimoroni Servo2040
; ---------------------------------------
[env:pimoroni_servo2040]
extends = env:base
board = pimoroni_servo2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25 -DSS=17

; ---------------------------------------
; Pimoroni Tiny2040
; ---------------------------------------
[env:pimoroni_tiny2040]
extends = env:base
board = pimoroni_tiny2040
build_flags = ${env:base.build_flags}
    -DTP_RGB_3PIN_LED=1
    -DTP_PIN_LED_R=18
    -DTP_PIN_LED_G=19
    -DTP_PIN_LED_B=20

; ---------------------------------------
; Pimoroni Tiny2350
; ---------------------------------------
[env:pimoroni_tiny2350]
extends = env:base
board = pimoroni_tiny2350
build_flags = ${env:base.build_flags}
    -DTP_RGB_3PIN_LED=1
    -DTP_PIN_LED_R=18
    -DTP_PIN_LED_G=19
    -DTP_PIN_LED_B=20

; ---------------------------------------
; Pintronix PinMax
; ---------------------------------------
[env:pintronix_pinmax]
extends = env:base
board = pintronix_pinmax
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; RAKwireless RAK11300
; ---------------------------------------
[env:rakwireless_rak11300]
extends = env:base
board = rakwireless_rak11300
build_flags = ${env:base.build_flags} -DTP_RGB_LED=0 -DTP_PIN_LED=23

; ---------------------------------------
; redscorp RP2040-Eins
; ---------------------------------------
[env:redscorp_rp2040_eins]
extends = env:base
board = redscorp_rp2040_eins
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; redscorp RP2040-ProMini
; ---------------------------------------
[env:redscorp_rp2040_promini]
extends = env:base
board = redscorp_rp2040_promini
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Generic Sea-Picro
; ---------------------------------------
[env:sea_picro]
extends = env:base
board = sea_picro
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Seeed INDICATOR RP2040
; ---------------------------------------
[env:seeed_indicator_rp2040]
extends = env:base
board = seeed_indicator_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; Seeed Xiao RP2040
; ---------------------------------------
[env:seeed_xiao_rp2040]
extends = env:base
board = seeed_xiao_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Seeed Xiao RP2350
; ---------------------------------------
[env:seeed_xiao_rp2350]
extends = env:base
board = seeed_xiao_rp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Silicognition RP2040-Shim
; ---------------------------------------
[env:silicognition_rp2040_shim]
extends = env:base
board = silicognition_rp2040_shim
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Soldered Electronics NULA RP2350
; ---------------------------------------
[env:soldered_nula_rp2350]
extends = env:base
board = soldered_nula_rp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Solder Party RP2040 Stamp
; ---------------------------------------
[env:solderparty_rp2040_stamp]
extends = env:base
board = solderparty_rp2040_stamp
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Solder Party RP2350 Stamp XL
; ---------------------------------------
[env:solderparty_rp2350_stamp_xl]
extends = env:base
board = solderparty_rp2350_stamp_xl
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Solder Party RP2350 Stamp
; ---------------------------------------
[env:solderparty_rp2350_stamp]
extends = env:base
board = solderparty_rp2350_stamp
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; SparkFun IoT Node LoRaWAN
; ---------------------------------------
[env:sparkfun_iotnode_lorawanrp2350]
extends = env:base
board = sparkfun_iotnode_lorawanrp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun IoT RedBoard RP2350
; ---------------------------------------
[env:sparkfun_iotredboard_rp2350]
extends = env:base
board = sparkfun_iotredboard_rp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun MicroMod RP2040
; ---------------------------------------
[env:sparkfun_micromodrp2040]
extends = env:base
board = sparkfun_micromodrp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun ProMicro RP2040
; ---------------------------------------
[env:sparkfun_promicrorp2040]
extends = env:base
board = sparkfun_promicrorp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun ProMicro RP2350
; ---------------------------------------
[env:sparkfun_promicrorp2350]
extends = env:base
board = sparkfun_promicrorp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun Thing Plus RP2040
; ---------------------------------------
[env:sparkfun_thingplusrp2040]
extends = env:base
board = sparkfun_thingplusrp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun Thing Plus RP2350
; ---------------------------------------
[env:sparkfun_thingplusrp2350]
extends = env:base
board = sparkfun_thingplusrp2350
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun XRP Controller (Beta)
; ---------------------------------------
[env:sparkfun_xrp_controller_beta]
extends = env:base
board = sparkfun_xrp_controller_beta
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; SparkFun XRP Controller
; ---------------------------------------
[env:sparkfun_xrp_controller]
extends = env:base
board = sparkfun_xrp_controller
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; uPesy RP2040 DevKit
; ---------------------------------------
[env:upesy_rp2040_devkit]
extends = env:base
board = upesy_rp2040_devkit
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; VCC-GND YD RP2040
; ---------------------------------------
[env:vccgnd_yd_rp2040]
extends = env:base
board = vccgnd_yd_rp2040
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Viyalab Mizu RP2040
; ---------------------------------------
[env:viyalab_mizu]
extends = env:base
board = viyalab_mizu
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1

; ---------------------------------------
; Waveshare RP2040 LCD 0.96
; ---------------------------------------
[env:waveshare_rp2040_lcd_0_96]
extends = env:base
board = waveshare_rp2040_lcd_0_96
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040 LCD 1.28
; ---------------------------------------
[env:waveshare_rp2040_lcd_1_28]
extends = env:base
board = waveshare_rp2040_lcd_1_28
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040 Matrix
; ---------------------------------------
[env:waveshare_rp2040_matrix]
extends = env:base
board = waveshare_rp2040_matrix
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040 One
; ---------------------------------------
[env:waveshare_rp2040_one]
extends = env:base
board = waveshare_rp2040_one
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040 PiZero
; ---------------------------------------
[env:waveshare_rp2040_pizero]
extends = env:base
board = waveshare_rp2040_pizero
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040 Plus 16MB
; ---------------------------------------
; [env:waveshare_rp2040_plus_16mb]
; extends = env:base
; board = waveshare_rp2040_plus_16mb
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16
; >>> ERROR: pins_arduino.h file missing

; ---------------------------------------
; Waveshare RP2040 Plus 4MB
; ---------------------------------------
; [env:waveshare_rp2040_plus_4mb]
; extends = env:base
; board = waveshare_rp2040_plus_4mb
; build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16
; >>> ERROR: pins_arduino.h file missing

; ---------------------------------------
; Waveshare RP2040 Plus
; ---------------------------------------
[env:waveshare_rp2040_plus]
extends = env:base
board = waveshare_rp2040_plus
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2040-Zero
; ---------------------------------------
[env:waveshare_rp2040_zero]
extends = env:base
board = waveshare_rp2040_zero
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2350 LCD 0.96
; ---------------------------------------
[env:waveshare_rp2350_lcd_0_96]
extends = env:base
board = waveshare_rp2350_lcd_0_96
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2350 PiZero
; ---------------------------------------
[env:waveshare_rp2350_pizero]
extends = env:base
board = waveshare_rp2350_pizero
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2350 Plus
; ---------------------------------------
[env:waveshare_rp2350_plus]
extends = env:base
board = waveshare_rp2350_plus
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; Waveshare RP2350-Zero
; ---------------------------------------
[env:waveshare_rp2350_zero]
extends = env:base
board = waveshare_rp2350_zero
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=16

; ---------------------------------------
; WIZnet W5100S-EVB-Pico
; ---------------------------------------
[env:wiznet_5100s_evb_pico]
extends = env:base
board = wiznet_5100s_evb_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W5100S-EVB-Pico2
; ---------------------------------------
[env:wiznet_5100s_evb_pico2]
extends = env:base
board = wiznet_5100s_evb_pico2
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W5500-EVB-Pico
; ---------------------------------------
[env:wiznet_5500_evb_pico]
extends = env:base
board = wiznet_5500_evb_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W5500-EVB-Pico2
; ---------------------------------------
[env:wiznet_5500_evb_pico2]
extends = env:base
board = wiznet_5500_evb_pico2
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W55RP20-EVB-Pico
; ---------------------------------------
[env:wiznet_55rp20_evb_pico]
extends = env:base
board = wiznet_55rp20_evb_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W6300-EVB-Pico
; ---------------------------------------
[env:wiznet_6300_evb_pico]
extends = env:base
board = wiznet_6300_evb_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet W6300-EVB-Pico2
; ---------------------------------------
[env:wiznet_6300_evb_pico2]
extends = env:base
board = wiznet_6300_evb_pico2
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25

; ---------------------------------------
; WIZnet WizFi360-EVB-Pico
; ---------------------------------------
[env:wiznet_wizfi360_evb_pico]
extends = env:base
board = wiznet_wizfi360_evb_pico
build_flags = ${env:base.build_flags} -DTP_RGB_LED=1 -DTP_PIN_LED=25
